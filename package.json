{
  "name": "projets_scrapping",
  "private": true,
  "scripts": {
    "start:api": "cd API && npm run dev",
    "start:webapp": "cd webapp && npm run dev",
    "dev": "concurrently \"npm run start:api\" \"npm run start:webapp\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}